{
  "_notes": [
    "Feature-gate config, read client-side by every page. Missing file, missing key,",
    "or an expired subscription (see below) are all treated identically: every",
    "feature behaves as if it were 'true'. This file lives in config/ specifically",
    "because that directory is NOT copied by the mirror-signal.yml / mirror-intel.yml",
    "workflows -- a mirrored deployment (The-Signal, Intel) simply won't have this",
    "file at all and will run fully open unless a config/site.json is added there",
    "directly, independent of this one. That's what makes this the basis for a real",
    "per-deployment subscription-tier system later: each deployed copy of the site",
    "can carry its own config/site.json with its own flags.",
    "",
    "Each value under \"features\" is normally a plain boolean. A feature that needs",
    "extra switches is an object instead: {\"enabled\": bool, ...extra switches}.",
    "Both shapes are handled by every page's loader. Extra switches in use:",
    "  - feed.publishers: per-source on/off (see fetch_feed.py's FEEDS list for the",
    "    canonical name spelling). A publisher missing from this object, or the",
    "    whole \"publishers\" key missing, defaults that publisher to enabled --",
    "    this only needs listing publishers you want OFF.",
    "  - feed/cve/breaches/attack/leaktracker/negotiation/podcast.max_items: caps",
    "    how many of that category show in a digest carousel. Can only ever",
    "    REDUCE the count below the page's own built-in default -- a value above",
    "    that default (or anything not a positive number) is ignored and the",
    "    page's default is used instead. feed and podcast have no fixed built-in",
    "    default (feed is a rolling time-window, podcast is one slide per show),",
    "    so max_items on those is honored as given rather than compared to a",
    "    ceiling that doesn't exist.",
    "  - podcast.media_playback: spotlight.html's in-page audio player vs.",
    "    linking out to the source (intel.html always links out; this only",
    "    matters on spotlight.html).",
    "",
    "\"ads\" is a simple on/off for the sponsored slides in the spotlight/intel",
    "digest carousels -- disabling it just removes them, no locked/teaser state.",
    "",
    "\"analytics\" governs the 5 chart/stat slides (CVE severity, CVE KEV watch,",
    "feed volume by source, top-10 CVEs by score, most active ransomware groups).",
    "Turning it off reduces them to a single locked teaser, same as any other",
    "feature. Each individual analytic ALSO depends on its own underlying data",
    "feature (e.g. the feed-volume chart needs \"feed\") and gets watermarked on",
    "its own if that specific feature is off, even while \"analytics\" itself and",
    "the other 4 charts stay fully visible.",
    "",
    "Every other feature, when disabled, still shows in the digest carousels but",
    "capped to a single locked/watermarked teaser item instead of its normal count,",
    "and the corresponding standalone page (where one exists) redirects to",
    "index.html with a 'Subscribe for Access' message -- see README's Feature",
    "gating section for the full behavior.",
    "",
    "\"subscription\" is shaped after JWT claims (sub/iss/iat/exp) on purpose, so a",
    "future version of this loader can swap a fetched JSON file for a verified JWT",
    "without changing the shape features are read in. None of it is enforced yet",
    "beyond exp (and only if it's actually set -- it defaults to null, i.e. never",
    "expires): alg is deliberately \"none\" (the real, defined JWT value for",
    "'unsecured, do not trust this for access control') until real signature",
    "verification exists."
  ],

  "subscription": {
    "tier": "unrestricted",
    "sub": null,
    "iss": null,
    "iat": null,
    "exp": null,
    "alg": "none",
    "signature": null
  },

  "features": {
    "feed": {
      "enabled": true,
      "max_items": null,
      "publishers": {
        "The Hacker News": true,
        "Krebs on Security": true,
        "BleepingComputer": true,
        "Dark Reading": true,
        "SecurityWeek": true,
        "The Record": true,
        "SANS ISC": true,
        "Microsoft Security Blog": true,
        "Unit 42": true,
        "NCSC UK": true
      }
    },
    "cve": { "enabled": true, "max_items": null },
    "breaches": { "enabled": true, "max_items": null },
    "attack": { "enabled": true, "max_items": null },
    "leaktracker": { "enabled": true, "max_items": null, , "watermark_victims": false },
    "negotiation": { "enabled": true, "max_items": null },
    "podcast": { "enabled": true, "media_playback": true, "max_items": null },
    "research": true,
    "reports": true,
    "dashboard": true,
    "spotlight": true,
    "ads": true,
    "analytics": true
  }
}
